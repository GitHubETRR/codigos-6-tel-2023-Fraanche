#include <stdio.h>

int main(void) {
  float flotante;
  char *puntero = (char *) &flotante;
  flotante = 11.25;
 
  printf("%.2x",*puntero);
  printf("%.2x",*(puntero+1));
  printf("%.2x",*(puntero+2));
  printf("%.2x",*(puntero+3));

  return 0;
}
